# Writing Style Rules

- Never use em dashes (—). Use regular dashes, commas, or colons instead.

# PDF Generation

- PDFs can be generated from markdown files using the npm script defined in package.json. Run `npm run pdf` to regenerate all PDFs.
